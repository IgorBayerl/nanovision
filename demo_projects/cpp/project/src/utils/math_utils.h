
#ifndef MATH_UTILS_H
#define MATH_UTILS_H

#include <stdexcept>

namespace MathUtils {

// A function with a loop and several branches.
bool is_prime(int n);

// A function that can be tested for some, but not all, inputs.
long long factorial(int n);

}  // namespace MathUtils

#endif  // MATH_UTILS_H