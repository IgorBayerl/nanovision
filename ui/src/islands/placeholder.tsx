export type IslandProps = Record<string, unknown>

export function FileDetailsIsland() {
    return <div className="text-muted-foreground text-sm">File details interactivity will appear here.</div>
}
